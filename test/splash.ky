import kyte.stl;

attribute position : vec2 $ POSITION;
attribute texcoord : vec2 $ TEXCOORD0;

sampler texture : sampler2D $ 0;

program splash
{
    vertex() -> vec3
    {
        return vec4(position, 0f, 1f);
    }

    fragment() -> vec4
    {
        return sample(texture, texcoord);
    }
}
