#pragma program("physics", vertex, fragment)

attribute position : vec3 $ POSITION;
attribute color : vec4 $ COLOR0;

uniform modelViewProj : mat4;

vertex() -> vec3
{
    return modelViewProj * vec4(position, 1.f);
}

fragment() -> vec4
{
    return vec4(color.xyz, 1.0f);
}
